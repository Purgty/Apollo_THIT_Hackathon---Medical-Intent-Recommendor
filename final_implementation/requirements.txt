# Apollo Clinical NLP Pipeline — Python Dependencies
# =====================================================
# Pinned to minor versions for reproducibility in clinical environments.
# Install inside the 'nlp' conda env:
#   conda activate nlp
#   pip install -r requirements.txt

# --- Core data & ML stack ---
pandas==2.2.3
numpy==1.26.4

# --- Sentence Transformers (embedding model) ---
sentence-transformers==3.3.1
torch==2.5.1                  # CPU-only; use torch+cu121 for GPU builds

# --- FAISS (vector similarity search) ---
faiss-cpu==1.9.0

# --- LLM API client (OpenAI-compatible for LM Studio) ---
openai==1.58.1
httpx==0.28.1                 # Async HTTP transport used by openai client

# --- API server ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Utilities ---
python-dotenv==1.0.1          # .env file loading
tenacity==9.0.0               # Retry logic with exponential backoff
rich==13.9.4                  # Beautiful terminal output for data pipeline scripts
tqdm==4.67.1                  # Progress bars

# --- Testing (optional but recommended) ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1                 # also used as test client for FastAPI
